naftiko: 1.0.0-alpha2
info:
  label: Azure App Service REST API — Operations
  description: 'Azure App Service REST API — Operations. 1 operations. Lead operation: Azure App Service REST API List operations.
    Self-contained Naftiko capability covering one Microsoft Azure App Service business surface.'
  tags:
  - Microsoft Azure App Service
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_APP_SERVICE_API_KEY: MICROSOFT_AZURE_APP_SERVICE_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-app-service-operations
    baseUri: https://management.azure.com
    description: Azure App Service REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.Web-operations
      path: /providers/Microsoft.Web/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure App Service REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_APP_SERVICE_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-app-service-operations-rest
    port: 8080
    description: REST adapter for Azure App Service REST API — Operations. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-web/operations
      name: providers-microsoft-web-operations
      description: REST surface for providers-Microsoft.Web-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure App Service REST API List operations
        call: microsoft-azure-app-service-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-app-service-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure App Service REST API — Operations. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: azure-app-service-rest-api
      description: Azure App Service REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-app-service-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
